export const API_URL = "https://ramiriqui.pockethost.io";

export const contextApi = "/api/collections"

export enum httpMethods {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  PATCH = "PATCH",
  OPTIONS = "OPTIONS",
  HEAD = "HEAD",
}

export enum collectionsNames {
  USERS = "users",
}

